module.exports = {
  root: true,
  env: {
    browser: true,
    node: true,
  },
  extends: [
    '@nuxtjs/eslint-config-typescript', // Конфигурация для TypeScript
    'plugin:nuxt/recommended',          // Рекомендации для Nuxt
    'prettier',                         // Интеграция с Prettier
  ],
  rules: {
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
  },
};